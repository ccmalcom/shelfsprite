// Adds the jest-dom matchers (toHaveAttribute, toBeInTheDocument, ...) used by
// component tests. Harmless for the node-environment suites, which simply never
// call them.
import '@testing-library/jest-dom';
