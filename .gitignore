# Secrets
.env

# Data, cache, and local DB
data/*.csv
data/*.db
data/*.db-journal
data/cache/
data/eval/

# Python
__pycache__/
*.pyc
*.bak
.venv/
venv/
.pytest_cache/
*.egg-info/
.DS_Store
.vscode/

# Next.js build output
.next/
out/
dist/
.vercel

# Local env. `.env` is already ignored above; `.env.example` stays tracked.
.env.*
!.env.example

# TypeScript build cache
tsconfig.tsbuildinfo

# Node deps / tool caches.
node_modules/

#UI Test

app/ui-test/

#claude docs
# docs/superpowers/ is tracked on purpose: the Node-migration specs, wave plans and
# verification ledgers are project history, not scratch. Only the tooling's own state
# directory below stays ignored.
.superpowers/
.vscode/
.agents/
# .claude/
.worktrees/

# design-sync (staged scripts, build output, machine state)
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
.design-sync-build/ds.css
node_modules/shelfsprite-frontend
types/
