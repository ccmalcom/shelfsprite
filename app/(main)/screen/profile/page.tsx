import { ProfileView } from '@/components/profile/ProfileView';

/** The same unified taste profile, inside the ScreenSprite section (spec §7.1), with film and TV stats. */
export default function ScreenProfilePage() {
  return <ProfileView section="screen" />;
}
